extends ../layout

block content
  .container
    h1.main-title 회원정보 수정
    form(action=`/users/${user._id}?_method=PUT`, method='POST')
      .form-group
        label(for='name') 이름
        input.form-control(type='text', id='name', name='name', placeholder='이름을 입력해주세요', value=user.name)
      .form-group
        label(for='email') 이메일 주소
        input.form-control(type='email', id='email', name='email', placeholder='이메일을 입력해주세요', value=user.email)
      .form-group
        label(for='interest') 관심분야
        input.form-control(type='text', id='interest', name='interest', placeholder='관심분야를 입력해주세요', value=user.interest)
      .form-group
        label(for='current_password') 현재 비밀번호
        input.form-control(type='password', id='current_password', name='current_password', placeholder='현재 비밀번호를 입력해주세요')
      .form-group
        label(for='password') 바꿀 비밀번호
        input.form-control(type='password', id='password', name='password', placeholder='바꿀 비밀번호를 입력해주세요')
      .form-group
        label(for='password_confirmation') 바꿀 비밀번호 확인
        input.form-control(type='password', id='password_confirmation', name='password_confirmation', placeholder='바꿀 비밀번호를 입력해주세요')
      a.btn.btn-outline-secondary(href='javascript:window.history.back();') 돌아가기 
      | 
      button.btn.btn-primary(type='submit') 확인
